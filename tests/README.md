# Tests

The current site has no test framework or dependency. Run the standard-library
validator from the repository root:

```sh
python3 scripts/validate_site.py
```

Future tests should remain dependency-free unless the owner explicitly changes
that project constraint.
